{
  "name": "pattern-library-tools",
  "version": "1.0.0",
  "description": "Tools for managing pattern library",
  "scripts": {
    "convert": "node convert-to-json.js",
    "viewer": "node build-viewer.js",
    "screenshots": "node generate-screenshots.js",
    "build": "npm run convert && npm run viewer",
    "serve": "cd ../viewer && python3 -m http.server 3000"
  },
  "dependencies": {
    "playwright": "^1.40.0"
  }
}
